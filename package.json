{
  "name": "textfilters",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Textfilters ecosystem — docs, benchmarks, and integration smoke tests.",
  "license": "MIT",
  "scripts": {
    "benchmark": "node scripts/benchmark.mjs",
    "check": "node scripts/check-ecosystem-drift.mjs && node scripts/check-ecosystem-drift.mjs --self-test"
  },
  "dependencies": {
    "@textfilters/core": "^0.3.2",
    "@textfilters/url": "^0.1.6",
    "@textfilters/email": "^0.2.7",
    "@textfilters/phone": "^0.1.5",
    "@textfilters/profanity": "^0.14.0",
    "@textfilters/spam": "^0.3.0"
  },
  "overrides": {
    "@textfilters/spam": {
      "@textfilters/core": "^0.3.1"
    }
  },
  "engines": {
    "node": ">=24"
  },
  "packageManager": "npm@11.16.0"
}
