{
  "name": "@textfilters/url",
  "version": "1.0.0",
  "type": "module",
  "description": "TypeScript URL and obfuscated link detector for content moderation, defanged domains, hxxp links, TLD matching, and safe censoring.",
  "license": "MIT",
  "keywords": [
    "text-filtering",
    "content-moderation",
    "moderation",
    "typescript",
    "javascript",
    "censoring",
    "redaction",
    "url-detection",
    "link-filter",
    "obfuscated-links",
    "defanged-links",
    "domain-detection"
  ],
  "sideEffects": false,
  "files": [
    "dist",
    "README.md",
    "LICENSE"
  ],
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run",
    "lint": "prettier README.md package.json src docs tests scripts --check",
    "format": "prettier README.md package.json src docs tests scripts --write",
    "benchmark:url": "node scripts/benchmark-url.mjs",
    "prepack": "npm run build",
    "pack:dry-run": "npm pack --dry-run",
    "smoke:dist": "node --input-type=module --eval \"const url = await import('./dist/index.js'); if (!url.filter.check('go to https://example.com')) throw new Error('Smoke check failed'); url.createUrlFilter({ allowedDomains: ['example.com'] }).censor('https://example.com', '#');\"",
    "check": "npm run lint && npm test && npm run build && npm run smoke:dist && npm run pack:dry-run"
  },
  "dependencies": {
    "@textfilters/core": "^1.0.0"
  },
  "engines": {
    "node": ">=24"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/textfilters/textfilters.git",
    "directory": "packages/url"
  },
  "bugs": {
    "url": "https://github.com/textfilters/textfilters/issues"
  },
  "homepage": "https://github.com/textfilters/textfilters/tree/main/packages/url#readme",
  "publishConfig": {
    "registry": "https://npm.pkg.github.com"
  }
}
